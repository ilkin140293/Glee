$( function(){

  $('.top-slider__inner').slick({

    arrows: false,
    dots: true,
    autoplay: true,
    autoplaySpeed:3000

  })

  var mixer = mixitup('.products__items');
  var mixer = mixitup('.design__items');
  
});